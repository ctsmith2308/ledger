// Side-effect imports — registers all handlers against the command/query buses
import './application/commands/login-user';
import './application/commands/register-user';
import './application/queries/get-user-profile';
